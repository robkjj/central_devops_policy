# Networking Rules:

- Public IP is not allowed 
- all assigned should be private and within the assigned VPC CIDR range
- default enable allowed ports as below
    - SSH
    - HTTP/HTTPS
    - RDP
    - SQL Server


 
 